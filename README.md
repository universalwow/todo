# UNI-7

## Overview

This repository contains a Phoenix (Elixir) app for Linear ticket `UNI-7` (新增todo列表).

## Development

The Phoenix app lives in `uni_7/`.

```bash
cd uni_7
mix deps.get
mix ecto.setup
mix phx.server
```

Then open `http://localhost:4000/` (it redirects to `/todos`).

